# For local (non-Docker) dev, point to the backend on localhost.
# When running in Docker, leave this unset so the frontend proxies to http://backend:8000 automatically.
VITE_BACKEND_URL=http://localhost:8000
